# 模型调试功能使用指南

## 功能概述

模型调试功能为模型广场新增了一个专门的调试页面，参考应用调试的布局风格设计，用于快速测试和调试LLM模型。

## 主要特性

### 1. 布局设计
- **左侧配置面板**：设置模型参数和系统提示词
- **右侧调试面板**：实时对话测试
- 布局风格与应用调试保持一致

### 2. 配置选项

#### 系统提示词
- 自定义系统提示词来定义模型的行为和角色
- 支持多行文本编辑
- 默认提示词：`你是一个专业的AI助手，请根据用户的问题提供准确、有帮助的回答。`

#### 模型参数
- **Temperature** (0-2)：控制输出的随机性，值越高越随机
- **Top P** (0-1)：核采样参数，控制输出的多样性
- **Max Tokens** (100-4000)：最大输出token数量
- **Enable Thinking**：启用思考模式（如模型支持）

### 3. 调试特性
- ✅ 实时流式响应
- ✅ 对话历史保留（仅在当前会话）
- ✅ 清空对话功能
- ✅ 不存储到数据库（纯调试用途）
- ✅ 支持多轮对话

## 使用方法

### 1. 进入调试页面
1. 访问模型广场（首页）
2. 选择一个提供商（左侧边栏）
3. 找到要调试的模型卡片
4. 点击"对话"按钮

### 2. 配置模型
1. 在左侧配置面板中设置系统提示词
2. 调整模型参数（Temperature、Top P、Max Tokens）
3. 根据需要启用思考模式

### 3. 开始调试
1. 在右侧输入框中输入测试问题
2. 按Enter或点击发送按钮
3. 查看模型的实时响应
4. 继续多轮对话测试

### 4. 清空对话
- 点击右上角的垃圾桶图标清空所有对话历史

## 技术实现

### 前端组件
- **文件位置**：`frontend/src/views/ModelDebugView.vue`
- **路由路径**：`/model-debug`
- **参数传递**：通过URL query参数传递模型名称和提供商ID

### 路由配置
```javascript
{
  path: '/model-debug',
  name: 'ModelDebug',
  component: ModelDebugView
}
```

### URL参数
- `model`: 模型名称（如 `gpt-4`）
- `provider`: 提供商ID

示例：`/model-debug?model=gpt-4&provider=1`

### 数据流
1. 从URL参数获取模型信息
2. 从Vuex store中查找对应的提供商和模型
3. 构建请求payload发送到 `/api/chat/completions`
4. 使用流式响应实时显示结果
5. 对话历史仅保存在组件内存中，不存储到数据库

## 与应用调试的区别

| 特性 | 模型调试 | 应用调试 |
|------|---------|---------|
| 用途 | 测试LLM模型 | 测试应用配置 |
| 系统提示词 | 可自由编辑 | 使用应用配置的提示词 |
| 参数配置 | Temperature, Top P, Max Tokens, Thinking | Temperature（应用配置） |
| 变量替换 | 不支持 | 支持 {{variable}} 格式 |
| 数据存储 | 不存储 | 不存储 |
| Function Schema | 不涉及 | 显示和复制 |

## 注意事项

1. **不存储对话**：所有对话仅在当前会话中保留，刷新页面后会丢失
2. **参数范围**：请根据模型的实际支持情况调整参数
3. **思考模式**：Enable Thinking功能取决于模型是否支持，不是所有模型都支持此功能
4. **流式响应**：使用SSE（Server-Sent Events）实现实时响应
5. **返回导航**：点击左上角返回按钮可回到模型广场

## 快捷键

- **Enter**：发送消息（在输入框中）
- **Shift + Enter**：换行（在输入框中）

## 故障排查

### 问题：无法加载模型
- 检查URL参数是否正确
- 确认提供商和模型是否存在
- 查看浏览器控制台错误信息

### 问题：无法发送消息
- 确认模型已正确加载
- 检查后端API是否正常运行
- 查看网络请求是否成功

### 问题：流式响应中断
- 检查网络连接
- 查看后端日志
- 确认API Key是否有效

## 未来改进

- [ ] 支持保存调试配置
- [ ] 支持导出对话历史
- [ ] 支持更多模型参数（frequency_penalty, presence_penalty等）
- [ ] 支持多模态输入（图片、文件等）
- [ ] 支持对话分支和版本管理
- [ ] 支持性能指标显示（响应时间、token消耗等）

---

**实现日期**: 2026-01-14  
**版本**: 1.0.0
